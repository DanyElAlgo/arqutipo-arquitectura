// DBcontext
public class RegisterProfessorUseCase
{
    register(DocenteDTO docente)
    {
        Docente docente = new Docente()
        {
            // Añadir datos con el DTO
        }
    }
    DBcontext.save()
}